# The following lines should parse without error.
owm_api_key = "42"
owm_city_id= "2615876"

fullscreen = yes #We want fullscreen.

# Comment identifier in the value is still part of the value.
test="#test"
